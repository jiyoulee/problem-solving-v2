#include <cstdio>

using namespace std;

int N, M;

int main() {
    /*
     * Input.
     */
    scanf("%d%d", &N, &M);
    
    /*
     * Output.
     */
    printf("%d", N * M - 1);
    
    return 0;
}