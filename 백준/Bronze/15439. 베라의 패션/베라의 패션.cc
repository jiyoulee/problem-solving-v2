#include <cstdio>

using namespace std;

int N;

int main() {
    /*
     * Input.
     */
    scanf("%d", &N);
    
    /*
     * Output.
     */
    printf("%d", N * (N - 1));
    
    return 0;
}