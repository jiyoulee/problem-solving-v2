#include <cstdio>

using namespace std;

int L;

int main() {
    /*
     * Input.
     */
    scanf("%d", &L);

    /*
     * Output.
     */
    printf("%d", (L + 4) / 5);

    return 0;
}